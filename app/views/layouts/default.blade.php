<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <title>@yield('title')</title>
        @yield('header')
    </head>
    <body>
       @yield('content')

       @yield('footer')
    </body>
</html>